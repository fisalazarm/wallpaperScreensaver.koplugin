local _ = require("gettext")

return {
    name = "wallpaper",
    fullname = _("Wallpaper desde portada"),
    description = _([[Cada vez que abres un libro, guarda su portada en /storage/emulated/0/Pictures/cover.png. Si abres el mismo libro que ya está guardado, no hace nada.]]),
}
